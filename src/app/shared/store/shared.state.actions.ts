export namespace SharedActions {
  export class ShowLoader {
    static readonly type = '[Shared] Show Loader';
  }

  export class HideLoader {
    static readonly type = '[Shared] Hide Loader';
  }
}
